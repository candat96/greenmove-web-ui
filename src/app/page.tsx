'use client';

import { StyledTable } from "@/components";

const Home = () => {

  const dataSource = [
    {
      key: '1',
      name: 'Mike',
      age: 32,
      address: '10 Downing Street',
    },
    {
      key: '2',
      name: 'John',
      age: 42,
      address: '10 Downing Street',
    },
  ];

  const columns = [
    {
      title: 'Name',
      dataIndex: 'name',
      key: 'name',
    },
    {
      title: 'Age',
      dataIndex: 'age',
      key: 'age',
    },
    {
      title: 'Address',
      dataIndex: 'address',
      key: 'address',
    },
  ];

  return (
    <StyledTable
      columns={columns}
      dataSource={dataSource}
      total={2}
      currentPage={1}
      pageSize={20}
      scroll={{ y: 500 }}
    />
  );
}

export default Home;